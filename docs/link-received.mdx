export const GridFlexRowCenter = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        flexWrap: "nowrap"
    }}>{props.children}</div>;

};


<GridFlexRowCenter>
    <div>
        # Someone sent you a snippet!
        The snippet is saved in your Pieces library along with its tags and related links.
    </div>

    ![](/assets/headers/light/link_received_header_snippet.png)

</GridFlexRowCenter>