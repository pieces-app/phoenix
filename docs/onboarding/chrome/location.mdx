export const InstallButton = props => {
    return <a style={{textDecoration: "none"}} target="_blank" href={ props.src }>
        <div style={{maxWidth: "fit-content", minWidth: "fit-content"}}>
            <p style={{
                padding: "8px 20px",
                borderRadius: "8px",
                border: "1px solid #f3f5f6",
                color: "white",
                fontWeight: "500",
                background: "black",
                marginBottom: "8px",
                marginTop: "3px",
                marginRight: "16px"
            }}>{props.children}</p>
        </div>
    </a>
};

export const GridFlexRowCenter = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        flexWrap: "nowrap"
    }}>{props.children}</div>;

};

export const Spacer = props => {
    return <div style={{height: "16px"}}></div>
};

export const HeaderSpacer = props => {
    return <div style={{width: "100%", height: "100px"}}></div>;
};

export const CenterFlexRow = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        flexWrap: "nowrap"
    }}>{props.children}</div>
};


<HeaderSpacer></HeaderSpacer>

<CenterFlexRow>
    # Woo-hoo! You saved your first snippet.
</CenterFlexRow>

<CenterFlexRow>
    Find your snippet in the Pieces desktop app (located in your applications folder). From the desktop app, you can see all of your snippets, which have been automatically enriched with tags, descriptions, related links and more.
</CenterFlexRow>

<GridFlexRowCenter>
    <InstallButton src={"pieces-for-developers://"}>View Snippet</InstallButton>
</GridFlexRowCenter>


<Image src={"/assets/graphics/chrome/onboarding_location_chrome.png"} />

<GridFlexRowCenter>
    <InstallButton src={"https://code.pieces.app/onboarding/chrome/welcome"}>Back</InstallButton>
    <InstallButton src={"https://code.pieces.app/onboarding/chrome/finished"}>Finish</InstallButton>
</GridFlexRowCenter>

<Spacer></Spacer>
