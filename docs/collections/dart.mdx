---
title: TypeScript Snippets Collection
description: These TypeScript snippets range from picking properties from an interface to removing falsy values. We made sure to offer all useful TypeScript code snippets.
---

<link rel="canonical" href="https://code.pieces.app/collections/typescript" />

export const HeaderSpacer = props => {
    return <div style={{width: "100%", height: "100px"}}></div>;
};

export const GridFlexRowCenter = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        flexWrap: "nowrap"
    }}>{props.children}</div>;
};

export const InstallButton = props => {
    return <a style={{textDecoration: "none"}} href={ props.src }>
        <div style={{maxWidth: "fit-content", minWidth: "fit-content"}}>
            <p style={{
                padding: "8px 20px",
                borderRadius: "8px",
                border: "1px solid #f3f5f6",
                color: "white",
                fontWeight: "500",
                background: "black",
                marginBottom: "8px",
                marginTop: "3px",
                marginRight: "16px"
            }}>{props.children}</p>
        </div>
    </a>
};

export const Tag = props => {
    return <div style={{ padding: "2px 16px", borderRadius: "20px", border: "1px solid gray", maxWidth: "fit-content", margin: "0px 8px" }}>
        { props.children }
    </div>
};

export const GridFlexRow = props => {
    return <div style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'start',
        flexWrap: "nowrap",
        marginBottom: "32px",
    }}>{props.children}</div>
};

<HeaderSpacer></HeaderSpacer>

<Image margin-top={"0px"} margin-bottom={"0px"} src={"/assets/graphics/collections/collections_typescript.png"} />

<Heading style={{textAlign: "center"}} type={"h1"}>Typescript Collection</Heading>

These TypeScript snippets range from picking properties from an interface to removing falsy values. We made sure to offer all useful TypeScript code snippets.

<InstallButton>Add to Pieces</InstallButton>

---


If you are a TypeScript developer, add any of these snippets to Pieces for some extreme time-saving shortcuts. Coding in TypeScript can be challenging and getting used to the intricacies of adding typing to traditional JavaScript can be quite confusing.

This collection of TypeScript examples is aimed at helping you perform common TypeScript and JavaScript operations. No matter your experience level, from beginner to advanced, the following TypeScript code samples will be useful for all developers.

### How to pick properties from interface in TypeScript

<GridFlexRow>
    <Tag>typescript</Tag><Tag>pick</Tag>
</GridFlexRow>

> Construct a new type based on partial properties of an interface.

```typescript
interface MyInterface {
  id: number;
  name: string;
  properties: string[];
}

type MyShortType = Pick;
```

Related Links

- [Typescript Utility Types](https://www.typescriptlang.org/docs/handbook/utility-types.html)
- [How to Use Pick in TypeScript](https://www.becomebetterprogrammer.com/typescript-pick-type)

<InstallButton src={"https://typescript.pieces.cloud/?p=93854ea258"}>Add to Pieces</InstallButton>

---

### How to use TypeScript to exclude a property from an interface

<GridFlexRow>
    <Tag>typescript</Tag><Tag>omit</Tag>
</GridFlexRow>

> Exclude properties from a given interface with this TypeScript snippet.

```typescript
interface MyInterface {
  id: number;
  name: string;
  properties: string[];
}

type MyShortType = Omit;
```

Related Links

- [Typescript Utility Types](https://www.typescriptlang.org/docs/handbook/utility-types.html)
- [Omit Type in TypeScript](https://www.becomebetterprogrammer.com/typescript-pick-type)

<InstallButton src={"https://typescript.pieces.cloud/?p=bcef4ea3d9"}>Add to Pieces</InstallButton>

---

### How to use TypeScript to get a value of an interface property

<GridFlexRow>
    <Tag>typescript</Tag><Tag>get value</Tag>
</GridFlexRow>

> Get value of an object with an interface using this TypeScript code sample.

```typescript
interface MyInterface {
  id: number;
  name: string;
  properties: string[];
}

const myObject: MyInterface = {
  id: 1,
  name: 'foo',
  properties: ['a', 'b', 'c']
};

function getValue(value: keyof MyInterface) {
  return myObject[value];
}

getValue('id'); // 1
getValue('count')
```

Related Links

- [Keyof TypeScript Snippets](https://www.typescriptlang.org/docs/handbook/2/keyof-types.html)

<InstallButton src={"https://typescript.pieces.cloud/?p=9dc44680f5"}>Add to Pieces</InstallButton>

---

### How to store a record as a string with interface in TypeScript

<GridFlexRow>
    <Tag>typescript</Tag><Tag>record</Tag>
</GridFlexRow>

> Map a string key to a custom interface in an object.

```typescript
const myTypedObject: Record = {
  first: {...},
  second: {...},
  ...
};
```

Related Links

- [Typescript Utility Types](https://www.typescriptlang.org/docs/handbook/utility-types.html)
- [Record Typed in TypeScript](https://stackoverflow.com/questions/51936369/what-is-the-record-type-in-typescript)
- [TypeScript Record Type Explained](https://betterprogramming.pub/typescripts-record-type-explained-691372b1a449)

<InstallButton src={"https://typescript.pieces.cloud/?p=ac9649b2bb"}>Add to Pieces</InstallButton>

---

### How to check if a collection of elements are true in TypeScript

<GridFlexRow>
    <Tag>typescript</Tag><Tag>every</Tag><Tag>collection</Tag>
</GridFlexRow>

> Returns true if all elements in a collection are true.

```typescript
const all = (arr: T[], fn: (t: T) => boolean = Boolean) => arr.every(fn);
```

Related Links

- [Array Prototype Every - TypeScript Code Snippets](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/every)
- [TypeScript Array Every](https://www.tutorialspoint.com/typescript/typescript_array_some.htm)

<InstallButton src={"https://typescript.pieces.cloud/?p=fb3d47bffb"}>Add to Pieces</InstallButton>

---

### How to check if at least one element passes in a function using TypeScript

<GridFlexRow>
    <Tag>typescript</Tag><Tag>some</Tag>
</GridFlexRow>

> Tests whether at least one element in the array passes the test implemented by a provided function.

```typescript
const some = (arr: T[], fn: (t: T) => boolean = Boolean) => arr.some(fn);
```

Related Links

- [Array Prototype Some - TypeScript example](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/some)
- [TypeScript Array Some Examples](https://www.tutorialspoint.com/typescript/typescript_array_some.htm)

<InstallButton src={"https://typescript.pieces.cloud/?p=5fc24784ca"}>Add to Pieces</InstallButton>

---

### How to remove falsy values from an array using TypeScript

<GridFlexRow>
    <Tag>typescript</Tag><Tag>filter</Tag><Tag>compact</Tag>
</GridFlexRow>

> Tests whether at least one element in the array passes the test implemented by a provided function.

```typescript
const compact = (arr: any[]) => arr.filter(Boolean);
```

Related Links

- [Combination of functions - Async + wait + setTimeout](https://stackoverflow.com/questions/33289726/combination-of-async-function-await-settimeout)
- [Using a setTimeout in an async function - Typescript Snippets](https://stackoverflow.com/questions/51200626/using-a-settimeout-in-a-async-function)
- [How to make TypeScript setTimeout async friendly](https://dev.to/bbarbour/making-settimeout-async-friendly-50je)

<InstallButton src={"https://typescript.pieces.cloud/?p=5fc24784ca"}>Add to Pieces</InstallButton>



